# rustfmt — stable 채널에서 동작하는 옵션만 쓴다.
# nightly 전용(imports_granularity, group_imports, wrap_comments …)을 넣으면
# CI 의 `cargo fmt --check` 가 조용히 무시하거나 경고를 뿜는다.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
