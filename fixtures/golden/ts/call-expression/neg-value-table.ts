// 값만 있는 표 — 부르는 자리가 하나도 없다.
export const SHIPPING = {
  standard: { fee: 3000, days: 3 },
  express: { fee: 5500, days: 1 },
};

export const FREE_OVER = 50000;
export const DEFAULT_METHOD = 'standard';
