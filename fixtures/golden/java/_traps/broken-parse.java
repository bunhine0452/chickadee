package com.ssafy.finalproject.security;

// 여는 괄호를 닫지 않아 파서가 복구 모드로 들어간다.
// 그 아래 줄들은 트리에 남지만 지목 토큰의 위치를 믿을 수 없다 (03 §2.3).
public class JwtUtil {
    void sign(String secret) {
        record(hmac(secret;
        record(hmac("x"));
    }
}
