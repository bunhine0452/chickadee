<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="shop.CartMapper">
  <select id="alive" resultType="Cart">
    SELECT id FROM cart WHERE deleted_at IS NULL AND id = #{cartId}
  </select>
</mapper>
