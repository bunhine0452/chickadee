; 단위는 행이다. 「몇 줄만」이라고 적은 자리 — 어느 줄인지는 이 절이 안 정한다.
; 구멍은 `LIMIT` 이고 곁에 그 수와 읽는 표가 잡힌다.
((from
   (relation (object_reference (identifier) @pick.3))
   (limit (keyword_limit) @pick.1 @hole (literal) @pick.2)) @site
 (#set! form "limit"))
