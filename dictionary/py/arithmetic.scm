; 셈하기 — `//` 를 **일부러 뺐다.** 이 개념이 자리를 버는 근거가 「나누기는 딱 떨어져도
; 소수를 낸다」(D152)인데 `//` 는 정수를 낸다. 잡으면 그 사용처에서 `rule` 이 거짓이 된다.
; yaml 의 `why` 도 `//` 를 「버림은 기호가 따로 있다」는 **대조**로 쓰지 사례로 쓰지 않는다.
; `//` 를 가르치려면 개념을 따로 세운다.
;
; 남는 한계 — 타입을 못 본다. `binary_operator` 는 `"a" + "b"`(이어붙이기)·`[0] * n`(되풀이)·
; `"%s" % x`(끼워넣기)도 담는다. 노드 종류를 부정하는 문법이 tree-sitter 쿼리에 없고,
; 텍스트 술어(`#not-match?`)로 흉내 내면 `f"…"`·이름 뒤의 값에서 다시 샌다. 걸러야 한다면
; 파생 층(`derive.ts`)에서 걸러야 하지 여기서 반쯤 거르면 안 된다.
((binary_operator
   left: (_) @pick.2
   operator: ["+" "-" "*" "/" "%"] @pick.1 @hole
   right: (_) @pick.3) @site
 (#set! form "arith"))
