/**
 * 채점 피드백 — packages/grading (t0 오답 · t1 줄 판정 · t2 티어).
 *
 * 키 이름은 `화면.자리` 다. `ko` 가 키 집합의 정본이고 `en/grading.ts` 가 따라온다 (D117).
 * **이 파일은 그 영역을 맡은 세션만 고친다** — 카탈로그를 영역별로 가른 이유가 그것이다.
 */
export const grading = {} as const;
