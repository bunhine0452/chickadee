/**
 * 세션 · 교정지 — components/{plate,session,t1,t2} · screens/session.
 *
 * 키 이름은 `화면.자리` 다. `ko` 가 키 집합의 정본이고 `en/session.ts` 가 따라온다 (D117).
 * **이 파일은 그 영역을 맡은 세션만 고친다** — 카탈로그를 영역별로 가른 이유가 그것이다.
 */
export const session = {} as const;
