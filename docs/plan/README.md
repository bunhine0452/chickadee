# 구현 계획 — 「자바 말고 다른 언어와 프로젝트도」

2026-09-05, 사용자 요청 「자바 말고도 다른 언어와 프로젝트들도 지원할 수 있는 걸 찾아보고
**구현할 계획을 세워** 달라」에 답하는 네 문서다. 병렬 세션 넷이 같은 날 같은 트리에서 각자
실측하고 썼다.

**넷 다 계획이지 착수가 아니다.** 결정 등록부(`docs/00-overview.md` §4.2.1)에 행을 올리지
않았고, 각 문서의 마지막 절에 **올려야 할 행의 초안**이 있다. 착수 결정은 사용자가 한다.

이 폴더는 마일스톤 회고(`docs/handoff/`)와 다르다 — 저쪽은 끝난 일이고 여기는 아직 안 정해진
일이다. 정본은 여전히 `.oculpm/discussion/vibe-code-study-app/discussion.md` 의 「결론」이고,
설계는 `docs/00-overview.md` 부터 읽는다.

---

## 넷을 어떤 순서로 읽나

**[`extension-model.md`](./extension-model.md) 를 먼저 읽는다.** 나머지 셋은 축 하나씩이고
이것은 축을 늘리는 **비용**이다 — 셋이 공통으로 지나야 하는 자리와, 그 자리를 미리 줄이는
방법이 여기 있다.

| 문서 | 축 | 한 줄 |
|---|---|---|
| [`extension-model.md`](./extension-model.md) | 뼈대 | 언어 하나에 손대는 자리가 **29곳**(필수 8). 정본 §5 의 「티어 A 로 내려앉는다」는 **실측하면 참이 아니다** — 안 덮은 언어는 빈 코스다. 배관 단일화 → 언어 센서스 → 골격 스크립트를 먼저 하면 축 셋이 고칠 자리가 20 → 3 |
| [`rust-axis.md`](./rust-axis.md) | 러스트 | *(F1 세션 — 도착하면 이 줄을 채운다)* |
| [`python-axis.md`](./python-axis.md) | 파이썬 | 사전 **8장**. 가장 싼 큰 승리는 프레임워크가 아니라 **중심 16장** — `adelie` 줄 덮음 37.6% → 75.9%. 둘째는 CLI 진입점 씨앗(챕터 0개 → 7개). 그리고 `py/arithmetic` 사용처의 **51%가 셈이 아니다**(지금 있는 버그) |
| [`js-framework-axis.md`](./js-framework-axis.md) | JS·TS 프레임워크 | 언어 어휘는 이미 가장 두껍다(`ts/` 38장). 빈 것은 그 위층 — 진입점이 **다섯 리포 중 넷에서 0개**다. `react/` 를 아홉으로, `node/` 신설, 라우트 규칙 넷, node 러너(자바보다 싸다), Astro 문법 |

---

## 넷이 같이 가리키는 것

세 축이 따로 재고 같은 결론에 닿은 자리가 있다.

- **러스트 예산이 31줄 남았다** (`bash scripts/check-rust-budget.sh` → 2,769/2,800).
  새 문법 하나가 `langs.rs` 에서 3줄이므로 **어느 언어에 그 줄을 쓸지가 결정거리**다.
  Go·Rust 는 문법이 이미 링크돼 있어 **Rust 코드 0줄**이고, C·C++·C#·Swift·Astro·Ruby 는 든다.
- **진입점이 없으면 기능 챕터가 안 선다.** 파이썬 축이 `adelie`·`ECC` 에서, JS 축이 다섯 중
  넷에서 같은 것을 쟀다. HTTP 라우트를 전제한 `entryUnits` 가 CLI·데스크톱 앱에서 빈 배열을 낸다.
- **프레임워크(티어 B)보다 언어 어휘(티어 A)가 먼저 값을 낸다.** 파이썬 축의 셈이 가장
  분명하다 — 중심 16장이 덮음을 두 배로 올리는데 프레임워크 사전은 그 절반의 절반도 못 올린다.

---

## 이 폴더의 규칙

- 한국어. 수치 없는 주장을 쓰지 않는다 — 못 쟀으면 「못 쟀다」고 적는다.
- 사용자 리포(`/Users/kimhyunbin/Desktop/git/`)는 **읽기만** 했다.
- 문서가 결정을 만들지 않는다. 등록부에 행이 올라가야 착수다(`CLAUDE.md`).
