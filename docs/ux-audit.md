# UX 감사 — 앱을 실제로 돌려 찾은 불편 (D170)

**결정 등록부** D170. 사용자 요청: 「지금 UX 관련해서도 불편함이 많아 이를 조사하고 chickadee 를
정교한 학습 소프트웨어가 될 수 있도록」.

2026-09-05, mockIPC 하네스(`tests/support/*` · D108)로 앱을 띄워 첫 실행 → 리포 등록 → 인제스트 →
홈 → 세션(T0 두 장 · 사다리 1~4단 · 오답 · 정답 · T2 영향 반경 · 요약) → 설정 → 서가 → 클론 코스
→ 야간반 → 최소 창(1000×680)을 눌렀다. 화면마다 스크린샷과 계측(13px 미만 활자 · 가로 넘침 ·
이름 없는 단추 · 포커스 · 스크롤 오프셋)을 남겼고, 그 파일 이름을 근거 열에 적었다. 계측 결과
13px 미만 활자 0 · 가로 넘침 0 · 이름 없는 단추 0 — 정본 §6 의 활자 규칙은 지켜지고 있다.
문제는 규칙 밖에서 났다.

**한계.** 시드는 `tiny`(파일 5 · 커밋 0) 하나라 대지·스티커·노드 상세·T1 필사 판은 화면으로
못 봤고 코드로만 봤다. 실제 Tauri 창은 병렬 세션 다섯이 같은 트리를 고치는 중이라 띄우지 않았다
— 감사 주행은 `HEAD` 워크트리에서 했다. 코스 화면(D162·D171)은 아직 없어 이 감사의 대상이 아니다.

---

## 1. 이미 보고된 것 — 무엇이 고쳐졌나

등록부 D126~D147 은 앱을 실제로 쓴 사용자의 보고에서 나왔다. 이번 주행에서 그 상태를 다시 봤다.

| 보고 | 행 | 지금 |
|---|---|---|
| 창 위 흰 띠 | D126 | 하네스로는 못 봄(Tauri 전용). 코드는 그대로 |
| 게이지가 안 보인다 | D127 | 고쳐짐 — 진행바 18px, 빈 칸 점무늬 (`B1-home`) |
| 정답이 늘 2번 | D128 | 고쳐짐 — 주행 두 판에서 1번·2번 |
| 판정문이 은유뿐 · 도장이 제목을 덮음 | D130 | 고쳐짐 — 「틀렸습니다」 평문 + 152px 상자 (`B9-verdict-wrong`) |
| 정답 모션이 이상하다(LIFER 베일) | D131 | 고쳐짐 — 판정란 안 블록 (`B11-verdict-right`) |
| 문제 수준을 모르겠다 | D132 | 게이트로 잼. 화면에는 안 보임(설계대로) |
| 리포가 클수록 창이 길다 | D133 | 고쳐짐 — 색인 띠 + 대지 한 장 (`B1-home-full` 1,703px) |
| 튜토리얼 | D134 | 고쳐짐 — 첫 판 안내 띠 3걸음 (`B3-plate1`) |
| Dock 아이콘 | D135 | 하네스로는 못 봄 |
| 코드가 일부분만 보인다 | D141 | 고쳐짐 — 감싸는 블록 창. 단 시드는 파일이 없어 한 줄 발췌만 보임 |
| 너무 언어 중심적 | D144·D145 | 사전 부채 게이트. 시드 판 넷 중 T2 한 장 — 아직 언어 중심 |
| 자동완성 | D143 | 클론 코스에서 확인 (`D3-course`) |

전부 반영돼 있다. 아래는 그 뒤에 남은 것과 새로 생긴 것이다.

---

## 2. 찾은 것 — 심각도 순

심각도: **막힘** = 학습을 직접 막거나 거짓을 보여 준다 · **헛걸음** = 사용자가 한 번 더 움직여야
한다 · **거슬림** = 읽을 때 걸린다. 처리: **고침** / **사용자 결정** / **A7**(코스 화면으로 넘김).

### 막힘

| # | 증상 | 재현 | 근거 | 규칙 | 처방 | 처리 |
|---|---|---|---|---|---|---|
| 1 | **판의 코드가 원본과 다르다.** `86_400_000` 이 `_400_000` 으로, `0xFF` 는 `xFF` 로 보인다 | 숫자 구분자·16진수·지수·BigInt 가 있는 줄의 판 | `B10-plate2.png` 코드 창 · `hl.ts:33` | 정본 §1 「내 코드가 교재」 | 숫자 토큰을 `\d[\w.]*` 로 넓히고, 어느 갈래에도 안 걸린 글자를 맨 글자로 남긴다 | **고침** `hl.ts` + 시험 2 |
| 2 | **다음 판의 머리가 작업 띠 밑에 숨는다.** Space 로 넘어가면 판 번호·개념·출처가 안 보이고 「먼저 읽기」 줄부터 보인다. 요약도 같다 | 판 하나를 채점(판정란까지 스크롤) → Space | `B10-plate2.png` · `B12-plate3.png` · `B13-summary.png` · `B10-scroll.txt` (`.bench` scrollTop 138 · `html` 17) | 정본 §3-9 「한 화면에 한 가지 일」 · 05 §7 「판이 걸리면 포커스는 교정지」 | 판이 바뀔 때 `.bench` 를 0 으로 되돌리고 `focus({ preventScroll })` | **고침** `ProofSheet.tsx` · `Summary.tsx` + 시험 1 |
| 3 | **토스트가 영원히 남는다.** T2 채점 문구 「채점했습니다. 꼭 고쳐야 할 3개 중 1개를 찾았습니다」가 요약과 홈까지 따라온다 | T2 판 채점 → Space → 홈 | `B12c` · `B13` · `B14-home-after.png` · `B14-toast.txt`(opacity 1) · `store.ts:213` | 정본 §3-9 | `say()` 가 `TOAST_MS`(3.6s) 뒤 스스로 지운다. 부르는 자리가 여덟이라 스토어가 시계를 든다 | **고침** `store.ts` + 시험 2 |

### 헛걸음

| # | 증상 | 재현 | 근거 | 규칙 | 처방 | 처리 |
|---|---|---|---|---|---|---|
| 4 | **홈의 판 수가 세션과 다르다.** 홈 「2판 · 약 4분」 → 인쇄 시작하면 「1 / 3 · 남은 시간 약 8분」. Esc 로 나오면 홈이 「3판 · 약 8분」으로 바뀐다 | 홈 → 인쇄 시작 | `B1-home.png` · `B3-plate1.png` · `C1-home-resume.png` · `data/session.ts:553` | 정본 §3-5 「진행바는 시간 비례 — 남은 양이 거짓말이 되면 안 된다」 | 미리보기가 T1·T2 자리를 리듬(`queue.track_cadence`)과 재료 유무로 근사한다. 굽지는 않는다 | **고침** `data/session.ts` + 시험 1 |
| 5 | **홈이 「이 리포로는 T2 를 짤 수 없습니다」라 하는데 세션은 T2 영향 반경 판을 낸다** | 커밋 0 리포 | `B1-home.png` 예고 · `B12-plate3.png` | 정본 §6 「모든 은유 옆에 평문」 — 평문이 거짓이면 더 나쁘다 | 커밋이 필요한 종은 책임 배치뿐이다. 문구를 그 종으로 좁힌다 | **고침** `home.forecastCannot` |
| 6 | **이미 읽은 리포에 「리포를 읽으면 대지가 깔립니다」** — 읽으라는 말이 된다. 「개념 0개 중 0개를 찍었습니다」도 판이 넷인 화면에서 거짓이다 | 폴더당 파일 3개 미만인 리포 | `B1-home.png` · `A4-home-empty.png` | 「빈 상태는 정직하게」(05 §2.1) | 읽은 뒤의 빈 상태는 **왜 없는지**(한 폴더에 파일 셋부터)와 「판은 그래도 짜인다」를 말한다. 대지가 없으면 대지 단위 개념 수를 세지 않는다 | **고침** `HomeScreen.tsx` · `home.noSheetsRead` · `home.boardNoteNoSheets` |
| 7 | **최소 창(1000×680)에서 작업 띠가 두 줄**이 되어 180px 를 먹고 판이 그만큼 잘린다 | 창 폭 ≤ 1080 | `C5-plate-min.png` · `JobBand.css` `@media (width <= 1080px)` | 05 「최소 창 1000×680」 | 「나가기」를 줄에 남기고 브랜드 부제를 접는다 | **고침** `JobBand.css` |
| 8 | **클론 코스를 열면 포커스가 `<body>`** — 바로 칠 수 없고 Tab 을 여러 번 눌러야 한다 | 서가 → 코스 열기 | `D3-course.metrics.json` focus=`body` · `CoursePlateView.tsx:135` `focusOnMount={false}` | 정본 §3-8 키보드 완결 | 조각이 걸리면 편집기(textarea)에 포커스 — 지연 로드된 Monaco 가 만들어지는 순간 `ClonePad` 가 든다 | **고침** `ClonePad.tsx` · `PlainPad.tsx` · `CoursePlateView.tsx` |
| 9 | **코스 목차의 파일 경로가 7자에서 잘려** `src/ses…` 둘이 구별되지 않는다 | 파일 다섯 이상인 코스 | `D3-course.png` 왼쪽 목차 | — | 파일 이름을 앞세우고 폴더는 뒤에 흐리게, 전체 경로는 `title` | **고침** `CourseToc.tsx` |
| 10 | **설정이 한 단 3,859px** 인데 「홈으로」가 맨 위에만 있다 | 설정 → 끝까지 스크롤 | `D1-settings.png` | — | 맨 아래에도 「홈으로」 | **고침** `SettingsScreen.tsx` |
| 11 | **홈 「잉크 겹」 패널이 기본 접힘**이라 세션 직후 「다시 찍을 개념」·「다음 인쇄」가 한 번 더 눌러야 보인다 | 세션 완료 → 홈 | `B14-home-after.png` | D133 (사용자 결정) | 접힘은 사용자 결정이다. 세션 직후 한 번은 펴 두는 것을 제안 | **사용자 결정** |

### 거슬림

| # | 증상 | 근거 | 처방 | 처리 |
|---|---|---|---|---|
| 12 | 판정란 대기 문구가 구현 규칙을 학습자에게 말한다 — 「이 칸은 미리 비워 둔 자리라 답해도 위쪽 글이 밀리지 않습니다」 | `B3-plate1.png` | 「답을 고르고 Enter 를 누르면 맞았는지, 왜 그런지, 잉크가 몇 겹 오르는지가 여기 적힙니다」 | **고침** `plate.idleNote` |
| 13 | 요약의 T2 행에 빈 `<code>` 상자가 그려진다 (T2 는 토큰이 없다) | `B13-summary.png` 「영향 반경 ▯」 | 토큰이 없으면 상자를 안 그린다 | **고침** `Summary.tsx` |
| 14 | 홈 미리보기 목록이 「새 판 새 판」 — 이름 자리와 곁말 자리가 같은 말 | `B1-home.png` | 이름 자리는 「새 문법 판」·「필사 한 판」·「구조 한 판」, 곁말은 역할 | **고침** (#4 와 함께) |
| 15 | 사다리 머리의 이득 표시가 「잉크 0겹 → 0겹」 — 내려갈 겹이 없을 때도 화살표를 그린다 | `B4-ladder-r1.png` | 0겹이면 「내려갈 겹 없음」 한 마디 | 남김 — e2e 골든(`t0-session.spec` 03)이 이 문구를 박제. 다음 판 |
| 16 | 첫 실행의 「프로그래밍이 처음이신가요?」가 「해 봤습니다」로 **이미 골라진 채** 뜬다. 코드 주석은 「안 고른 것을 아니오로 세지 않는다」인데 화면은 그렇게 보인다 | `A1-firstrun.png` · `empty.tsx:60` | 셋째 상태(미선택)를 둘지, 기본값을 「처음입니다」로 둘지 | **사용자 결정** — 스위치 모양이 디자인 결정 |
| 17 | 세션 뒤로 홈이 **원래 밝기로** 비친다 — 오버레이 밖 좌우 여백에서 「인쇄 시작」 단추와 범례가 그대로 읽힌다 | `B3-plate1.png` 좌우 | 책상 은유(교정쇄가 홈 위에 놓인다)라면 의도. 다만 정본 §3-9 와 긴장 | **사용자 결정** — 시각 방향 |
| 18 | **WKWebView 에서 Tab 이 단추를 건너뛴다** — macOS 「모든 항목에 Tab 으로 이동」이 꺼져 있으면 `Option+Tab` 만 돈다. 키보드 완결(정본 §3-8)이 시스템 설정을 탄다 | `tests/support/ui.ts` `tabKey` 주석 (실측 Tab 12번에 포커스 불변) | 첫 실행 문단에 한 줄 안내하거나, 단추에 `tabindex=0` 이 WebKit 에서 도는지 실기 확인 | **사용자 결정 · 실기 확인 필요** (하네스는 Chromium) |
| 19 | 시드가 파일을 못 읽으면 판이 **발췌 한 줄**만 보인다 — `1 \| 86_400_000` 처럼 줄 번호 1 에 토큰만 | `B10-plate2.png` | 하네스 한계(픽스처 리포가 디스크에 없다). 실제 앱은 D141 창을 그린다 | 기록만 |
| 20 | **e2e-ui·gates 게이트가 `HEAD` 에서 이미 빨갛다** — D140 이 첫날 큐에 T2 한 장을 넣고 D147 이 첫 화면에 스위치를 하나 더 세웠는데 시나리오 여섯(`keyboard 15` · `shell 11·13·14` · `t0-session 02`)과 게이트 일곱(`design 요약 ×4` · `keyboard ×2` · `en-smoke`)이 그대로였다. 헬퍼 `finishSession`·`toSummary` 가 T2 판에서 「안 끝난 T0 판이 없다」로 던진다 | 워크트리 기준선 실측(pristine `HEAD`: e2e-ui 5 실패 · gates 7 실패) | 헬퍼가 T2 판을 **키보드로** 지나가고(`passT2Plate`), Space 뒤에는 판 이름표가 바뀔 때까지 기다린다. 시나리오 수치를 T0 둘 + T2 하나로 고친다 | **고침** `tests/support/{ui,gates}.ts` · 시나리오 6 |
| 21 | 작업대 끝에서 휠이 **뒤의 홈으로 샌다** — 세션 중 `html` 이 17~51px 움직였다(계측). 오버레이는 고정이라 안 보이지만 홈이 뒤에서 흔들린다 | `B10-scroll.txt` `html: 17` | `.bench { overscroll-behavior: contain }` | **고침** `SessionOverlay.css` |

---

## 3. 코스(D162)로 넘어가면 지금 화면과 부딪히는 자리 — A7(코스 화면)에 넘긴다

코스는 단위가 **기능**이고 진도가 **단(stage)**이다. 지금 화면은 단위가 판이고 진도가 겹이다.
아래는 그대로 두면 두 축이 한 화면에서 서로 다른 말을 하게 되는 자리다.

| 자리 | 지금 | 코스에서 |
|---|---|---|
| 홈 「오늘의 인쇄」 | 판 N장 · 분 | 「오늘 챕터의 다음 단」 + 「만기 재검」 두 줄. 판 수는 그 안의 수치 |
| 작업 띠 진행바 | 판 단위 시간 비례 | 단 단위(읽기·추적·예측·수정) 시간 비례. 관문(어휘 6판)은 앞에 붙는 칸 |
| 판 머리 「N판 · T0 · 의미형 · 새 판」 | 트랙·유형 | 「로그인 · 2단 추적 · hop」 — 챕터·단·유형. `T0/T1/T2` 알약은 단 뒤로 |
| 잉크 겹(스티커·레일·요약) | 주 진도 | 1·2단 어휘 판정기로 강등(D162 ④). 시각은 남기되 진도 자리에서 뺀다 — 얼마나 지울지는 **사용자 결정**(`docs/program/mastery.md` §8) |
| 마스트헤드 「코스」 단추 | 클론 코스(D120 · T1 필사) | 이름이 겹친다. 새 코스가 「코스」면 클론 코스는 「통째 필사」로 |
| 요약 「오늘 움직인 잉크」 | 겹 변화 목록 | 「오늘 오른 단」이 먼저, 겹은 그 아래 |
| T2 지도 판 | 대지 단위 종 여섯 | 코스 2단 `hop` 과 3단 `contract` 가 같은 지도를 쓴다. 지도 컴포넌트(`DependencyMap`)는 재사용, 판 껍데기는 단의 것 |
| 「모르겠어요」 사다리 | T0 4단 | 단마다 처방이 다르다(`mastery.md` §5) — 2단은 경로 접기, 4단은 프롬프트. 사다리 껍데기에 단별 몸통 |
| 세션 재진입 「이어 찍기 · N번째 판부터」 | 판 위치 | 「로그인 · 2단부터」 |

**App.tsx · flow.ts 에 필요한 배선**(이 감사는 손대지 않았다): 홈 → 코스 목차 화면 진입 · 세션
오버레이가 판 대신 단을 걸도록 `startSession` 의 짝 · 요약 뒤 챕터 통과 판정 호출.

---

## 4. 고친 것 — 검증

| 무엇 | 파일 | 시험 |
|---|---|---|
| #1 숫자 토큰·글자 보존 | `components/plate/hl.ts` | `hl.test.ts` +2 — 구분자·16진수·지수·BigInt, 조각을 이으면 원문 |
| #2 판·요약 전환 시 작업대 0 | `components/plate/ProofSheet.tsx` · `components/session/Summary.tsx` | `ProofSheet.test.tsx` +1 — scrollTop 138 → 0, 새 판에 포커스 |
| #3 토스트 자동 소거 | `store.ts` | `store.test.ts` +2 — `TOAST_MS` 뒤 사라짐 · 연달아 부르면 마지막 시계만 |
| #4 · #14 미리보기 T1·T2 자리 | `data/session.ts` | `session.test.ts` +1 갱신 1 — 대지가 있으면 구조 한 판, 굽지 않음 |
| #5 · #6 · #12 문구 | `packages/i18n/src/{ko,en}/{home,session}.ts` · `screens/home/HomeScreen.tsx` | `HomeScreen.test.tsx` · `Forecast.test.tsx` · `tests/e2e-ui/t1-t2.spec.ts` 갱신 |
| #7 띠 한 줄 | `components/session/JobBand.css` | 워크트리 주행 `C5-plate-min` 재촬영 |
| #8 코스 포커스 | `components/t1/ClonePad.tsx` · `PlainPad.tsx` · `screens/clone/CoursePlateView.tsx` | 워크트리 주행 `D3-course.metrics.json` focus = `textarea.inputarea` |
| #9 목차 경로 | `screens/clone/CourseToc.tsx` · `.css` | `CloneScreen.test.tsx` 갱신 |
| #10 설정 맨 아래 홈으로 | `screens/settings/SettingsScreen.tsx` · `.css` | `SettingsScreen.test.tsx` 갱신 — 둘 다 같은 문 |
| #13 빈 코드 상자 | `components/session/Summary.tsx` | 기존 시험 통과 |
| #20 게이트 헬퍼 | `tests/support/ui.ts` · `tests/support/gates.ts` · `tests/e2e-ui/{keyboard,shell,t0-session,t1-t2}.spec.ts` | e2e-ui 34 · gates 114 통과(두 엔진) |
| #21 휠 새기 | `components/session/SessionOverlay.css` | stylelint 통과 · 워크트리 주행 |

### 검증 결과 (2026-09-05)

주행과 게이트는 **`HEAD` 워크트리에 이 변경만 얹어** 돌렸다 — 공유 트리는 병렬 세션 다섯이
고치는 중이라 그 순간의 빌드가 매번 달랐다(실제로 감사 중 시드가 스키마 8로 바뀌며 하네스가
첫 실행 화면으로 떨어졌다).

| 게이트 | 결과 |
|---|---|
| `vitest` apps/desktop · packages/ui · packages/i18n | 116 파일 942 통과 (+ 새 시험 7) |
| `tsc` apps/desktop · tests | 무출력 |
| `eslint` · `stylelint` 고친 파일 | 무출력 |
| `check:contrast` · `check:motion` · `design:check` | 48쌍 통과 · 위반 0 · 바이트 일치 |
| `playwright tests/e2e-ui` chromium + webkit | 34 통과 · 12 건너뜀 (기준선: 5 실패) |
| `playwright tests/gates` chromium + webkit | 114 통과 · 8 건너뜀 (기준선: 7 실패) |
| 워크트리 재주행 | 판 머리 보임(`psTop 152 > bandBottom 112`) · 토스트 `opacity 0` · 코스 포커스 `textarea.inputarea` |

공유 트리의 `pnpm test:unit`·`typecheck`·`lint` 전체 결과는 일지에 적는다 — 다른 세션의
진행 중인 파일이 섞여 이 감사만의 수치가 아니다.

---

# 2차 감사 — 여정을 걷고 막힘을 게이트로 (D186 ①·④)

2026-09-05, 첫 물결(형식 둘 · 사전 셋 · SQL 러너 · 그림 일곱)이 들어온 뒤. 1차(D170)는 세션
화면이 대상이었고 코스 화면(D171)은 그때 없었다. 이번에는 **코스가 여정의 본선**이라 첫 실행 →
홈 → 세션 → 코스 → 챕터 → 단 오버레이를 순서대로 걸었다.

주행은 mockIPC 하네스(`tests/support/*` · D108)에 Playwright chromium 을 붙여 했고, 화면마다
**포커스 · 보이는 단추 · 본문 전문**을 찍어 남겼다. 재는 것은 D186 ① 이 정한 셋이다 —
막힘(다음 행동이 없는 화면) · 되돌아가야만 나갈 수 있는 곳 · 첫 판까지의 클릭 수.

**한계.** 시드는 `tiny` 하나뿐이고(`projectox` 에는 `blocks.json` 밖에 없다) 그 리포로는
코스 판이 한 장도 안 구워진다 — 요청 줄기 0 · 값이 두 번 바뀌는 자리 0 · 커밋 0. 그래서
단 오버레이 안쪽은 **판 하나를 손으로 심어** 봤고, 4·5단(수정·재구현)은 러너가 없어 못 봤다.
실제 창(Tauri)은 리눅스 전용이라 여기서 안 띄웠다 — 같은 길을 `tests/e2e/specs/e9-journey.e2e.ts`
가 걷는다.

## 1. 실측

| 재는 것 | 전 | 후 |
|---|---|---|
| 막힘(다음 행동이 없는 화면) | **3** — 코스 「오늘 15분」 · 판 없는 챕터 · 오늘 몫을 다 푼 홈 | **0** |
| 포커스가 `<body>` 로 떨어지는 자리 | **5** — 첫 실행 · 홈(첫 부팅) · 코스 · 설정 · 서가 | **0** |
| 화면이 거짓을 말하는 자리 | **5** (아래 표 #1·#3·#5·#6·#7) | **0** |
| 첫 실행 → 첫 판 클릭 | **3** (리포 등록 → 홈으로 → 학습 시작) + OS 폴더 선택 1 | **2** (리포 등록 → 학습 시작) + OS 폴더 선택 1 |
| 홈 → 첫 판 클릭 | 1 | 1 (게이트 상한 2) |
| 코스 → 첫 단 클릭 | 2 (코스 → 오늘 시작) | 2 |

되돌아가야만 나갈 수 있는 곳은 전·후 모두 **0** 이다 — 화면마다 「홈으로」가 있고 세션·단
오버레이는 Esc 하나다(정본 §3-4). 이번 주행에서 새로 깨진 자리는 없었다.

## 2. 찾은 것 — 심각한 순

심각도: **막힘** = 다음에 뭘 해야 할지 화면이 안 말한다 · **거짓** = 화면이 사실과 다른 말을
한다 · **헛걸음** = 한 번 더 움직여야 한다 · **거슬림** = 읽을 때 걸린다.

| # | 심각도 | 증상 | 근거 | 처방 | 처리 |
|---|---|---|---|---|---|
| 1 | 막힘 · 거짓 | **코스 「오늘 15분」이 「코스가 끝났거나 리포를 읽기 전입니다」** 라고 말하고 단추를 하나도 안 낸다. 실제로는 챕터가 남았고 리포도 읽은 뒤다 | 주행 `10-course` — 계획 0칸, 주 단추 0 | 「없다」의 이유를 넷으로 가른다(다 밟음 · 1단 판정만 남음 · 판을 못 구움 · 그 밖) — 이유마다 문장과 **다음 행동**이 다르다 | **고침** `CourseScreen.tsx`(`todayGap`) · `chapter.todayNoneDone`·`todayReading`·`todayNoCards`·`reingest` |
| 2 | 막힘 | **판이 없는 단**에서 「리포를 다시 읽으면 채워집니다」라고만 하고 다시 읽는 문이 없다 | 주행 `11-after-reading` | 그 자리에 「리포 다시 읽기」를 낸다 | **고침** `ChapterPanel.tsx` · `CourseScreen.tsx` |
| 3 | 막힘 · 거짓 | **오늘 몫을 다 푼 홈**에 주 단추가 없고 「리포를 더 파거나 내일 다시 오세요」가 어디를 파라는 말인지 안 가리킨다 | 게이트 `세션을 끝내면…` 실패 | 억지로 채우지는 않는다(정본 §3) — 대신 **문 둘을 이름으로** 가리키고(「아직 안 배운 문법」·「코스」) 게이트는 그 끝 표식(`.today-off`)을 다음 행동으로 친다 | **고침** `home.todayEmpty` · `journey.spec.ts` |
| 4 | 막힘 | **포커스가 `<body>`** — 첫 실행·코스·설정·서가에 들어가면 Tab 이 브라우저 크로뮴부터 돈다. 홈도 **첫 부팅**에서 그랬다(효과가 도는 순간 `.press` 가 아직 없다) | 주행 전 화면 `focus: body` | `Page` 에 `focusOnMount`, 첫 실행은 `<main>` 에 직접, 홈은 효과의 의존성에 `ui.home` 을 넣고 **잃었을 때만** 옮긴다 | **고침** `Page.tsx` · `empty.tsx` · `App.tsx` · 화면 넷 |
| 5 | 거짓 | **합성 판이 「내 코드 사전 예제:1」** 이라고 적는다. 「네 코드엔 없다」 사유 넷(`t0.absent*`)은 카탈로그에만 있고 **아무 화면도 안 불렀다** | `t0-synthetic.ts` 주석 「지금은 그 키 넷이 아직 없어서…」 · grep 0건 | 판이 합성이면(`card.site_id` 가 NULL) 출처 줄을 「사전 예제 — 내 코드에서 떠 온 줄이 아닙니다」로 바꾸고, 리포에 자리가 아예 없는 개념이면 **왜 없나**를 한 줄 붙인다 | **고침** `T0Plate.tsx` · `t0.syntheticSource` |
| 6 | 거짓 | **첫 판 안내가 합성 판에서 거짓말** — 「위에 있는 코드는 … 당신 리포에서 그대로 떠 온 줄이에요」 | 위와 같은 주행 | 합성 판에서만 다른 첫 걸음 문장 | **고침** `CoachBand.tsx` · `coach.pickSynthetic` |
| 7 | 거짓 | **「0 / 0 챕터 통과」** — 목차에는 챕터가 하나 보이는데 셈은 0/0 이고 0% 막대가 선다. 기능 챕터만 세고 막간·부록을 안 센다 | 주행 `10-course` | 기능 챕터가 0개면 셈과 막대를 안 그리고 「기능 챕터 없음 · 막간·부록 N개」라고 적는다. 빈 「챕터」 절도 안 그린다 | **고침** `ChapterToc.tsx` · `chapter.countExtraOnly` |
| 8 | 거슬림 | **판정란에 「규칙 — 규칙 — …」** — 판정란이 말머리를 붙이는데 새 형식 둘의 문구가 이미 「규칙 — 」로 시작한다 | 주행 `13-stage-graded-0` | 카탈로그에서 말머리를 뗀다 | **고침** `chapter.orderRule` · `chapter.traceRule` |
| 9 | 거슬림 | **「이 줄이 끝난 뒤 · 채점 = 0% —」** — 코스의 채점 줄이 T0 의 「이 줄이 끝난 뒤」 칸을 빌려 써 말머리도 꼬리표도 안 맞고 빈 주석 자리에 「—」가 매달린다 | 같은 주행 | 판정란에 채점 줄(`score`)을 따로 두고, 「이 줄이 끝난 뒤」는 주석이 비면 「—」를 안 그린다 | **고침** `FeedbackSlot.tsx` · `PlateFrame.tsx` |
| 10 | 헛걸음 | **인제스트가 끝나면 「홈으로」 하나** — 다음 행동은 학습인데 홈을 한 번 거친다 | 첫 실행 클릭 셈 3 | 「학습 시작」을 주 단추로 두고 「홈으로」는 옆에 남긴다 | **고침** `IngestScreen.tsx` · `App.tsx` |
| 11 | — | **헤더의 밝기 스위치** (D187 ⑫ 위반). 기본이 「시스템 따름」도 아니었다 | `Topbar.tsx` | 헤더에서 빼고 설정에 라디오 셋(시스템 따름 · 밝게 · 어둡게), 기본은 시스템 따름이며 「시스템 따름」인 동안만 `prefers-color-scheme` 을 듣는다 | **고침** `Topbar.tsx` · `SettingsScreen.tsx` · `data/settings.ts` |

## 3. 1차 감사(D170)에서 「남김」이었던 것 — 지금

| 1차 # | 그때 | 지금 |
|---|---|---|
| 11 홈 「잉크 겹」 패널 기본 접힘 | 사용자 결정 | **없어졌다** — D182 가 그 패널을 지웠다 |
| 15 사다리 머리 「0겹 → 0겹」 | 남김(e2e 골든이 문구를 박제) | **그대로 남음** — 골든이 아직 그 문구를 든다. 다음 판 |
| 16 첫 실행 「해 봤습니다」가 이미 골라진 채 | 사용자 결정 | **그대로 둔다** — 스위치는 둘 중 하나가 늘 켜져 있어야 하고, 셋째 상태는 「0장 길이 하나」를 정하는 문항에 비해 무겁다. 아래 한 줄이 「잠기는 것은 없다」고 말한다 |
| 17 세션 뒤로 홈이 원래 밝기로 비친다 | 사용자 결정 | **그대로 둔다** — 책상 은유이고, `inert` 로 보조 기술에는 안 잡힌다. 시각은 S2 소관 |
| 18 WKWebView 에서 Tab 이 단추를 건너뛴다 | 사용자 결정 · 실기 확인 필요 | **완화됐다** — 화면마다 `<main tabindex=-1>` 에 포커스를 두므로 첫 걸음이 시스템 설정을 안 탄다. 그 뒤의 Tab 순회는 여전히 엔진 정책이다 |
| 19 시드가 파일을 못 읽어 발췌 한 줄 | 기록만 | 그대로 — 하네스 한계다 |

## 4. 게이트 — 막힘 0 을 시험으로

| 파일 | 무엇을 재나 | 시험 수 |
|---|---|---|
| `tests/gates/journey.spec.ts` | 첫 실행 → 홈 → 첫 판 → 요약 → 코스 → 1단 → 2단 오버레이. 화면마다 **주 단추 하나 이상**(또는 「끝났다」 표식)과 **포커스가 `<body>` 가 아님**. 첫 판까지의 클릭 상한 2 | 8 × 2엔진 |
| `tests/gates/honesty.spec.ts` | 앱이 못 하는 넷의 문구가 ko·en 에 있고 **화면이 그 키를 실제로 부르는가**. `RunnerReason` 은 소스에서 갈래를 읽어 전수. 합성 판 하나는 하네스로 띄워 문구가 보이는 것까지 | 7 × 2엔진 |
| `tests/e2e/specs/e9-journey.e2e.ts` | 같은 길을 실제 창에서 (리눅스 CI). 리포 등록 구간은 E2 와 같은 이유로 못 걷는다 | 6 |

두 게이트 모두 **못 잡는 것을 머리글에 적었다** — 정적 대조는 「키가 소스에 글자로 있다」까지고,
브라우저 하네스는 Tauri 창의 포커스 정책을 대신하지 못한다.

## 5. 검증

| 게이트 | 결과 |
|---|---|
| `pnpm typecheck` | 내 파일 무출력 |
| `pnpm lint` | 내 파일 무출력 |
| `pnpm test:unit` (apps/desktop · packages/i18n · packages/ui) | 103 파일 976 통과 |
| `playwright tests/gates/journey` + `honesty` (chromium + webkit) | 30 통과 |
| `playwright tests/gates` 전체 | 173 통과 · 10 건너뜀 |
| `playwright tests/e2e-ui` | 26 통과 · 12 건너뜀 |

## 6. S2(디자인 QA)가 넘긴 마크업 다섯 — 고침

S2 가 스크린샷 144장을 찍고 넘긴 것. 1번이 실제 회귀였다.

| # | 증상 | 뿌리 | 처방 |
|---|---|---|---|
| 1 | **어둡게가 설정 화면에서만 걸린다** — 144장 중 어둡게 23화면이 밝게와 바이트까지 같았다 | `applyTheme()` 을 부르는 곳이 `useAppearance()` 훅뿐인데, D187 ⑫ 로 헤더 스위치가 빠지면서 그 훅을 든 화면이 설정 하나가 됐다. `index.html` 의 `data-theme="light"` 못박기가 CSS 의 시스템 따름(`:root:not([data-theme="light"])`)까지 막았다 | `boot.ts` 가 창을 보이기 전에 `startTheme()` — 저장된 `theme_mode`(기본 `system`)를 읽어 세우고, 「시스템 따름」인 동안 `prefers-color-scheme` 을 듣는다. `index.html` 의 못박기 삭제 |
| 2 | 5단에서 **판은 밝고 Monaco 만 검다** | 세 화면이 Monaco 테마를 `settings.theme` 에서 읽었다 — 그 열은 **마지막으로 고른 것의 기록**이라 「시스템 따름」인 채 한 번도 안 고르면 기본 `light` 다 | `useResolvedTheme()` 신설 — `<html data-theme>` 을 읽고 `applyTheme` 이 알린다. `SessionScreen`·`CloneScreen`·`CourseScreen` 이 그것을 쓴다 |
| 3 | 판 머리가 **「내 코드 a」** | `OrderPlate` 가 조각 **id** 를 파일 경로 자리에 넣었다 | 조각의 보이는 글자(`t`)로 가른다 — 홉 재료면 `파일:줄`, 0부 사다리면 「사전 예제」(`t0.syntheticSource`) |
| 4 | 5단에서 **「시그니처」가 30px 사이로 두 번** | `Ask` 의 곁말과 `<dt>` 가 같은 키를 썼다 | 곁말은 **무엇을 하라**로 — `chapter.reimplAsk` |
| 5 | 「값 추적 판을 굽지 못했습니다」가 판 위에 **가운데 정렬**로 뜬다 | `.bench` 의 직계 자식이라 판 밖에 따로 섰다 | `PlateFrame` 의 `after` 로 판 **안**에 넣었다 — 「이 화면에 종이 한 장」(정본 §3-9) |

**검증 (`pnpm shots` 재주행).** 밝게·어둡게 **72쌍 전부 바이트가 다르다** (전: 23쌍 동일).
회귀는 게이트로 잠갔다 — `journey.spec.ts` 의 「시스템이 어두우면 설정에 들어가지 않아도 홈이
어둡다」와 「설정에서 고르면 시스템이 바뀌어도 안 따라간다」 둘(×2엔진).
