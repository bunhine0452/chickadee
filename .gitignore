# oculpm:begin v1
.oculpm/index/
.oculpm/hooks/
.oculpm/.lock
.oculpm/.schema-version
.oculpm/oculpm.log
.oculpm.backup-*/
# oculpm:end

# secrets
.env
.env.*
!.env.example

# build
/target/
node_modules/
dist/
.vite/
*.tsbuildinfo

# fixtures — .steps 만 커밋, 생성물은 CI 가 만든다 (06 §1.2)
/fixtures/repos/*/

# 벤치 — 기준선만 커밋, 이번 실행 결과는 기계마다 다르다 (06 §1.6)
/bench/current.json

# insta — `.snap` 은 골든이라 커밋하고, 실패했을 때 옆에 떨어지는 후보는 커밋하지 않는다
*.snap.new

# macOS
.DS_Store

# python 캐시 (design/src/ink/build.py)
__pycache__/
*.pyc
